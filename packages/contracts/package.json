{
  "name": "@pulse/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Somnia Shannon on-chain attestation contracts for Pulse",
  "scripts": {
    "compile": "mkdir -p artifacts && npx solc --bin --abi -o artifacts src/PulseAudit.sol",
    "deploy": "tsx scripts/deploy.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "viem": "^2.21.54"
  }
}


